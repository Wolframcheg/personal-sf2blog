<a href="#" class="easyadmin-thumbnail ff" data-featherlight="#easyadmin-lightbox-{{ uuid }}" data-featherlight-close-on-click="anywhere">
    <img src="{{ vich_uploader_asset(item, field_options.uploadField) }}">
</a>

<div id="easyadmin-lightbox-{{ uuid }}" class="easyadmin-lightbox">
    <img src="{{ vich_uploader_asset(item, field_options.uploadField) }}">
</div>