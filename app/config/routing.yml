#admin:
#    resource: "@AppBundle/Controller/Admin"
#    type:     annotation
#    prefix:   /%admin_prefix_route%

easy_admin_bundle:
    resource: "@EasyAdminBundle/Controller/"
    type:     annotation
    prefix:   /%admin_prefix_route%

auth:
        resource: "@AppBundle/Controller/AuthController.php"
        type:     annotation
        prefix:   /%admin_prefix_route%

elfinder:
     resource: "@FMElfinderBundle/Resources/config/routing.yml"

front_sitemap:
    resource: "@AppBundle/Controller/Front/SitemapController.php"
    type:     annotation

front_main:
    resource: "@AppBundle/Controller/Front/MainController.php"
    type:     annotation

logout:
    path: /%admin_prefix_route%/logout